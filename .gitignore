# Claude Code local files
.claude/

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/

# Original payment screenshots — contain FULL wallet addresses.
# Never commit these. Only the censored copies in /proof are published.
Screenshot *.png

# Temp crop/verify files and the censoring script
_*.png
_censor.py

# Raw WhatsApp screenshots & source videos — may contain names, emails,
# phone numbers, wallet addresses, anti-phishing codes. NEVER publish raw.
# Only web-optimised, censored copies in /proof go to the repo.
WhatsApp Image *.jpeg
WhatsApp Image *.jpg
*.mp4
# ...except web-optimised demo clips published in /assets
!assets/*.mp4
_censor_bybit.py
